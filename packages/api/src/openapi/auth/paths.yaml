/auth/login:
  post:
    security: []
    tags:
      - User Authentication 🔐
    summary: Log in to an existing account 🔑
    description: |
      Authenticates a user by validating their credentials and returning a token for accessing protected resources.

      This token grants access to various features of the platform, such as managing academies, user accounts, and more.
      Ensure that the correct credentials are provided to avoid authentication failures. 🔐
    x-eov-operation-handler: authController
    operationId: login
    requestBody:
      $ref: '#/components/requestBodies/LoginRequestBody'
    responses:
      200:
        $ref: '#/components/responses/Token'
      400:
        $ref: '#/components/responses/BadRequest'
      404:
        $ref: '#/components/responses/NotFound'
      500:
        $ref: '#/components/responses/InternalServerError'
